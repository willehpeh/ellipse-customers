import { ellipseContactsInfrastructure } from './ellipse-contacts-infrastructure';

describe('ellipseContactsInfrastructure', () => {
  it('should work', () => {
    expect(ellipseContactsInfrastructure()).toEqual(
      'ellipse-contacts-infrastructure'
    );
  });
});
