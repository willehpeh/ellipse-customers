import { ellipseContactsDomain } from './ellipse-contacts-domain';

describe('ellipseContactsDomain', () => {
  it('should work', () => {
    expect(ellipseContactsDomain()).toEqual('ellipse-contacts-domain');
  });
});
